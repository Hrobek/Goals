<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- Export compliance, answered once here so App Store Connect stops asking on every upload.
	     False is the honest answer: the app does no cryptography of its own - just HTTPS and the
	     standard OS services (CloudKit, the analytics upload). Revisit if the app ever encrypts
	     data itself. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<!-- CloudKit (SwiftData sync): 'remote-notification' delivers change pushes, 'processing'
	     lets NSPersistentCloudKitContainer run its scheduled export/import in the background.
	     Without the permitted-identifier below it logs "BGSystemTaskSchedulerErrorDomain Code=3". -->
	<key>UIBackgroundModes</key>
	<array>
		<string>remote-notification</string>
		<string>processing</string>
	</array>
	<key>BGTaskSchedulerPermittedIdentifiers</key>
	<array>
		<string>com.apple.coredata.cloudkit.activity</string>
	</array>
	<!-- HealthKit: only asked for the specific metrics a habit/goal is actually linked to, never a
	     blanket request. Share is for the metrics the app writes on the user's behalf (water,
	     mindful minutes, weight); Update is HealthKit's key for exactly that same write access. -->
	<key>NSHealthShareUsageDescription</key>
	<string>Goals reads the health metrics you link to a habit or goal (like steps or workouts) so it can check them off for you automatically.</string>
	<key>NSHealthUpdateUsageDescription</key>
	<string>Goals writes to Health only for habits or goals you set to log there yourself, like water intake or weight.</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.hrobek.goals</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>goals</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
